#include <bits/stdc++.h>
#define ll long long
using namespace std;

void find(int n) {
  ll sum = (n*(n+1))/2;
  if(sum & 1) {
    cout << "NO\n";
    return;
  }
  
  vector<int> dp(n+1);
  for(int )
}

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  
  int n;
  cin >> n;

  find(n);
  
  return 0;
}